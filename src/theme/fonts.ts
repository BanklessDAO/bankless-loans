import '@fontsource/space-grotesk'
import '@fontsource/clear-sans'

const fonts = {
    heading: 'Space Grotesk, sans-serif',
    body: 'Space Grotesk, sans-serif',
    buttons: {
        max: 'Clear Sans, sans-serif',
    },
}

export default fonts
