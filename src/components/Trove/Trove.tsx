
import { Opening } from "./Opening";

export const Trove = (props: JSX.IntrinsicAttributes): JSX.Element => {
//   const { view } = useTroveView();

//   switch (view) {
//     case "OPENING": {
      return <Opening {...props} />;
//     }
//   }
};
